interface:
  display_name: "CPA Image Generation"
  short_description: "Generate images through configured CPA / CLIProxyAPI"
  default_prompt: "Use $cpa-imagegen to generate the requested image through my configured CPA endpoint and save it in this project."
